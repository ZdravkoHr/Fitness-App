import styled from 'styled-components';

const PanelEl = styled.div`
	& {
		margin: 1.5rem 0;
	}

	.panel-header {
		padding: 8px 4px;
		background: #d4d2d2;
		border-radius: 5px;
		cursor: pointer;
	}
`;

export default PanelEl;
